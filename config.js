var config = {
  startingPrompt: `You are a translator, you will be taking in a JSON object with three things, a starting language, an ending language, and and the text to translate. Please ONLY output the result of the translation.`, 
}

module.exports = config